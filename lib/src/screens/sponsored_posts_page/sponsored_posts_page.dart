import 'package:flutter/material.dart';

class SponsoredPostsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Sponsored Posts"),
      ),
      body: Text("Sponsored posts"),
    );
  }
}
